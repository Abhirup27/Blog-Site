import { Injectable } from "@nestjs/common";
import { post } from "../types/post.type";



@Injectable()
export class PostService
{
    public getAllPosts = (userId: number): post => {
    
        return
    }
}